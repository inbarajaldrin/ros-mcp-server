---
context: phase
phase: 07-gripper-urdf-rviz
task: null
total_tasks: null
status: paused_before_planning
last_updated: 2026-05-02T12:57:25.275Z
---

# Resume Point — Compliant Insertion Studio

**Status:** Project initialization complete. About to start Phase 7. Operator paused work to step away.

<current_state>
Phase 7 (Gripper URDF + RViz Visualization) was just added but is **not yet planned**. The next action when resuming is `/gsd-plan-phase 7` to break it into concrete tasks. We were about to start Phase 7 because it's the highest-value `[N]` (no-robot) deliverable: it replaces the operator-described "annoying to maintain" custom DH-based `GRIPPER_CENTER_TOOL_OFFSET` with a URDF that lets RViz natively show the gripper geometry on every pose preview.

Background context: this session's work split into project initialization (PROJECT.md, REQUIREMENTS.md with 69 reqs, ROADMAP.md with 7 phases, CONVENTIONS.md, research outputs) + Phase 1 no-robot half (11 of 17 reqs done — folder scaffold, calibration script, smoke test, three-layer SOP, all reference articles).
</current_state>

## Required Reading (in order, before any code action)

1. `.planning/codebase/CONVENTIONS.md` — project working pattern. Read all of "Key Rules" — every agent must follow these. Two non-obvious ones:
   - **Research before code** (5–15 min `WebSearch` + `WebFetch` before any non-trivial deliverable; clone repos to `_references/repos/`, save articles to `_references/articles/`)
   - **Two-track split [N] / [R] / [N→R]** (the operator goes between at-robot and away sessions; both tracks must keep moving)
2. `.planning/TRACKS.md` — what's actionable right now per track. Top of away-track is Phase 7. Top of at-track when operator returns is the SETUP-01 + CAL-03 sequence (~25 min total).
3. `.planning/PROJECT.md` — Core Value: "Replace failing `--insert` real-mode path." All work serves this.
4. `.planning/REQUIREMENTS.md` — every requirement is now tagged `[N]`/`[R]`/`[N→R]`. 11 done, 6 [N] ready (TELE), 40 [N→R], 12 [R].
5. `.planning/ROADMAP.md` Phase 7 section — Goal, anticipated requirements, success criteria sketch.
6. `_references/articles/ft_payload_calibration_math.md` — only relevant if revisiting calibration; not needed for Phase 7.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Writing SOPs/specs from extrapolation | Mid-session this happened with the F/T calibration SOP — wrote 100+ lines of confident-sounding doc based on shallow grounding before researching what UR5e calibration actually means. Operator caught it. | blocking | Per CONVENTIONS §1: 5–15 min `WebSearch`/`WebFetch` + clone reference repos to `_references/repos/` BEFORE writing any non-trivial deliverable. The first thing to do for Phase 7 is search for USD→URDF tooling, OnRobot RG2 URDFs that may already exist (community-published), and ros2_control gripper integration patterns. |
| Custom DH-based collision setup | The current `GRIPPER_CENTER_TOOL_OFFSET = [0, 0, 0.2286]` in `primitives/shared/config.py` is a 229mm hardcoded offset used everywhere the gripper-tip position matters. Operator explicitly called this "annoying to maintain". Phase 7 exists to replace it with URDF FK lookups. | advisory | Don't extend the DH approach. Phase 7's deliverable is to make the URDF the single source of truth for gripper geometry. |
| Spawning subagents for routine work | This project's CONVENTIONS §"Inline-default" rule: routine code/planning happens in the main conversation. The 4-researcher init was the right subagent use case (independent + parallel + protect-context). Anything else, ask first. | advisory | Default to inline. Workflow toggles in `.planning/config.json` are already set to false (research/plan_check/verifier/code_review/pattern_mapper). |
| Running robot moves without RViz preview | Earlier in the session a calibration pose set was taken to real-hardware robot without IK/collision verification. Operator caught it before damage. | blocking | Per CONVENTIONS safety rules: every robot motion gets fake-hardware RViz preview first. The calibration script supports `--motion-only` flag for this. |

## Infrastructure State

- **ur_bringup (fake hardware)**: PID 668318 — running. Was used for calibration pose RViz previews. **Action on resume**: kill if not continuing immediate RViz work; restart fresh if resuming Phase 7 visualization.
- **rviz2**: PIDs 668344, 668452 — running, pointed at fake-hardware bringup's robot_description.
- **grasp_points_publisher (real mode)**: PID 390584 — running but stale. Real bringup was killed earlier this session, so this is publishing into nothing useful. **Action on resume**: kill it (`kill 390584`).
- **No real-robot bringup is up.** Operator killed it earlier to release u_brown and switch to fake hardware.
- **u_brown is on the table** somewhere — gripper was opened to release it, then real bringup killed.

## Two-Track Status

### `[N]` no-robot — work this when away from robot

Top priority: **Phase 7 — Gripper URDF + RViz Visualization** (run `/gsd-plan-phase 7` first).

Then in order: Phase 2 wrapper extension + TELE schema → Phase 4 dashboard scaffolding → Phase 6 dispatcher code.

See `.planning/TRACKS.md` for full breakdown.

### `[R]` robot-required — work this when operator returns

Top priority sequence (from TRACKS.md "When operator returns"):
1. SETUP-01 — `apt upgrade ros-humble-ur-robot-driver ros-humble-ur-msgs` (~5 min)
2. CAL-03 — run `ft_calibration.py --gripper-id <name>` → paste `set_target_payload(...)` line into bringup → restart bringup once (~15 min)
3. SETUP-02 + SETUP-03 verification on real robot (~5 min)
4. WRAP verification: full wrapper end-to-end run on known-good u_brown (~15 min)
5. **Phase 1 done.** Begin Phase 3 — DATA collection (~60–90 min)

## Decisions Made This Session

- **Two-track work split [N]/[R]/[N→R]** established and embedded in CONVENTIONS, REQUIREMENTS, TRACKS, CLAUDE.md.
- **Phase 7 added** — decoupled from Phases 1–6, recommended pulled forward.
- **8-pose calibration set** (`face_down_canonical`, `face_forward_w2_zero`, `side_pointing_pos_x`, `side_pointing_neg_x`, 4 oblique tilts) with LSQ condition number 1.64. Dropped two unsafe poses caught by gripper-tip preflight (face_up at z=0.92m overhead, face_left_w2_pi at y=+0.19m over robot base).
- **F/T calibration is three layers**: foundational (per-mount, recovers mass+CoG via Kubus 2007 LSQ) + session smoke (zero+5s confidence check) + per-pose zero (bias subtraction in wrapper PRE phase). Per-pose alone does NOT substitute for foundational.
- **All deliverables under `compliant_insertion_studio/`** — droppable into other robotics projects with one-line edit at host's `translate_object:1085` equivalent.
- **Workflow agents disabled** in `.planning/config.json` to enforce inline-default working pattern.
- **Reference repos cloned to `_references/repos/`**: `force_torque_tools` (KTH), `rq_fts_ros2_driver`, KIT `force_torque_sensor`. Articles saved as MD: F/T control ROS2, payload/CoG URScript, F/T calibration math derivation, URCap Measure wizard.

## Pre-Flight Patches Committed (pre-existing-codebase fixes from this session)

Three patches in commit `6afa019`:
- `primitives/rotate_object.py:622` — removed real-mode collision-check short-circuit
- `primitives/core/move_to_clear_area.py:_verify_grasp` — skip pose-lookup in real mode (AprilTag occluded by gripper)
- `primitives/translate_object.py:142` — `--width-only` for inner verify_grasp

## Recent Commits

```
95447d6 docs: split work into [N] / [R] / [N→R] tracks
78dfb7b docs: add Phase 7 — Gripper URDF + RViz Visualization
de21453 feat(CAL-01..10): foundational F/T payload calibration + three-layer SOP
f02909f feat(SETUP-04..07,CAL): scaffold compliant_insertion_studio + project conventions
4393452 docs: add SETUP-07 (migrate existing compliant_insert.py to wrapper/)
0df1404 chore: disable workflow agents (inline-default working pattern)
8b51dbe docs: add project guide (CLAUDE.md from gsd workflow)
6afa019 fix: pre-flight patches for real-mode insert pipeline
```

<context>
The session's mental model: get the project to a state where the operator can keep working from anywhere (away or at-robot), with maximum value extracted from each session type. Phase 7 is the bridge — once the gripper is in URDF, every subsequent calibration/wrapper/dashboard step gets visual collision verification "for free" instead of relying on the brittle custom DH preflight. The operator just paused right before Phase 7 planning would begin, so the resuming agent should pick up at `/gsd-plan-phase 7`.
</context>

<next_action>
Pause complete. When operator returns:

1. **If still away from robot**: Run `/gsd-plan-phase 7` to break Phase 7 (Gripper URDF + RViz) into concrete tasks. First task in the plan should be research (per CONVENTIONS §1): search for USD→URDF tools, existing OnRobot RG2 URDFs, and ros2_control patterns for adding a gripper to an existing arm URDF. Save findings to `_references/articles/`. Then execute inline.

2. **If at robot**: Run the at-robot sequence from `.planning/TRACKS.md` "When the operator returns" section — SETUP-01 → CAL-03 → SETUP-02/03 verification → WRAP verification. Phase 7 can be deferred but it's still the better long-term investment.
</next_action>
