from mcp.server.fastmcp import FastMCP

mcp = FastMCP("Prompts")

@mcp.prompt()
def phase_1_disassembly_sequence_discovery() -> str:
    """
    Returns a prompt for Phase 1: Disassembly sequence discovery in a simulation environment.
    """
    return """**Initialize:**

You are working in a sim environment.
Find the objects available in the scene. Save scene.
Identify available objects and their grasp ids.
You will be provided the fully assembled assembly in the scene.
You are performin a long horizon task so any change you make to the current state is important to the states that follow.
Check for any pre-existing resources that might help you perform the disassembly.

**Task**

Your goal is to disassemble the assembly thereby figuring out which grasp ids and gripper states let you perform the disassembly.
You can refer the instruction manual to figure out the sequence in which you need to disassemble them.
The information you collect in this run will be used later to perform assembly.

**Execution**

Use existing tools to perform disassembly one object at a time. Understand the capabilities of the tools and the limitations of the environment.
Gripper is in open mode by default. Half open the gripper before moving to grasp any object, accounting for limited space when accessing the objects already assembled.
If the object was successfully disassembled, log the tool call sequence that worked if no prior resource exits and then save scene state and log the grasp id and gripper state using which you were able to disassemble that object. Move on to the next object.
If it was a FAILURE, restore scene state and try a different grasp id or gripper state of the same object.
Perform the tasks using individual tools to figure out a working sequence of tool calls for one object.
You can orchestrate the tools to perform a composed policy to disassemble the remaining objects to automate the process using the working sequence.
Save results of disassembly.
"""

@mcp.prompt()
def phase_2_assembly_sequence_discovery() -> str:
    """
    Returns a prompt for Phase 2: Assembly sequence discovery with hypothesis formation in a simulation environment.
    """
    return """**Initialize:**

You are working in a sim environment.
Find the objects available in the scene. Save scene.
Read the disassembly results (generated by disassembling a fully assembled object) to identify the grasp ids that were successfully used per object during disassembly.
Use only the grasp id from the disassembly results to perform assembly. DO NOT try a different id.
You are performin a long horizon task so any change you make to the current state is important to the states that follow.
Check for any pre-existing resources that might help you perform the assembly.

**Task**

Your goal is to perform assembly of the objects scattered in the scene onto the base object based on the disassembly data collected earlier.
The same tool sequence might not work for all objects.

**Execution**

Read the tool descriptions properly and understand the underlying logic and sequence of the tools and their tool calls.
Use available tools to perform assembly.

Half-open gripper after securing the object into the base as to not disturb the previously placed parts.
If it was a SUCCESS, make sure the previously placed object wasn't disturbed and record the tools and arguments executed in sequence, save scene state and move on to the next object.
If it was a FAILURE, restore scene state and record the tools and arguments executed in sequence that didn't work and retry a different sequence you haven't tried before.
If IK fails, nothing is lost, you still have the object attached to the end effector and you can try a different sequence without restoring the scene state.
DO NOT orchestrate the tools to perform a composed policy.

**Troubleshooting**

If you're having trouble, try reading the object pose before and after tool calls and compare against the target pose of the object and reason on what the tool does to the object and the end effector orientation.
Read the tool description properly to not miss any details on how to use the tool.

**Knowledge Formation**

After completing assembly, analyze manipulation data to identify behavioral patterns.
Form explicit hypotheses about patterns observed during the task.
If there is an existing hypothesis, validate the sequence and if it works for a new assembly, promote the hypothesis to a rule.
If you don't see any new patterns, continue without this step.

"""

@mcp.prompt()
def phase_3_perform_assembly_sequence(mode: str = "real") -> str:
    """
    Returns a prompt for performing assembly based on assembly log data.
    
    Args:
        mode: The environment mode - either "sim" or "real" (default: "real")
    """
    environment = "sim environment" if mode == "sim" else "real environment"
    world_context = "simulation" if mode == "sim" else "real world"
    
    return f"""**Initialize:**

You are working in a {environment}.
Read the assembly sequence log to identify the tool call sequences for each object to perform assembly.

**Task**

You are to perform assembly of the objects onto a fixed base in the {world_context} based on the data assembly sequence resource collected using a Digital twin from your previous runs.

**Execution**

Use the available tools to perform assembly using the information of the assembly log in the same order. Do not skip any object assembly sequence.
Follow the sequence of objects and the tool calls with arguements to perform assembly one by one.
When performing assembly onto the base, half open gripper as to not disturb the previously placed parts.
Verify assembly after each object assembly.
Move home after each object is assembled.
Don't orchestrate the tools to perform a composed policy. Perform the tasks using individual tools calls. 

**Verification**

> To verify if an assembly was successful: run verify assembly once you you've ran all tools required to move one object into the fixed base.
> 1.**SUCCESS**: verify assembly returns success.
> 2.**FAILURE**: verify assembly returns failure. Pause and Request for assistance on further instructions.

**Output**

Fully assembled Assembly in the {world_context}."""

@mcp.prompt()
def camera_recording() -> str:
    """
    Returns a prompt for performing tasks with camera recording enabled.
    """
    return """Before starting the process:
1. Identify camera topics
2. Start recording from all camera topics

After completing the process:
3. Stop recording from all camera topics"""


if __name__ == "__main__":
    mcp.run()